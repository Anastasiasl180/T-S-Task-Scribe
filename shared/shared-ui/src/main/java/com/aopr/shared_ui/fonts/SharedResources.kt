package com.aopr.shared_ui.fonts

import com.aopr.shared_ui.R

object SharedResources {

    enum class Fonts(val id: Int) {
        CABIN(R.font.cabin), INTER(R.font.inter), OPEN_SANS(R.font.open_sans_light), ROBOTO(R.font.roboto)
    }

}